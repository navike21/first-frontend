export interface HeaderTranslations {
  language: { label: string }
  expandMenu: string
  collapseMenu: string
  guestName: string
  guestEmail: string
  userMenu: {
    ariaLabel: string
    profile: string
    preferences: string
    themeDark: string
    themeLight: string
    logout: string
  }
  settingsDrawer: {
    title: string
  }
}
