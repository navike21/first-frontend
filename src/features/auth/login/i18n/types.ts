export interface LoginTranslations {
  title: string
  subtitle: string
  form: {
    username: string
    password: string
    submit: string
  }
  validation: {
    usernameMin: string
    passwordMin: string
  }
}
