export interface LoginTranslations {
  title: string
  subtitle: string
  form: {
    email: string
    password: string
    submit: string
  }
  validation: {
    emailInvalid: string
    passwordMin: string
  }
}
