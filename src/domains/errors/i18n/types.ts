export interface ErrorTranslations {
  forbidden: {
    heading: string
    message: string
    loginButton: string
  }
  notFound: {
    heading: string
    message: string
    backButton: string
    homeButton: string
    loginButton: string
  }
  serverError: {
    heading: string
    message: string
    retryButton: string
  }
}
